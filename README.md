# AUTOMATION_VLAN_CREATION
I created this Python script to automate VLAN creation on access switches. It reads device details from a JSON file, connects using Netmiko over SSH, checks if the VLAN already exists, and creates it only when needed. It also handles authentication and timeout errors to ensure the process is safe and reliable.



# THIS IS CHANGED

